
=link_to basket_path(basket), class: 'list-group-item' do
  .media
    .media-body
      -if(basket.user)
        %h4.media-heading
          =basket.user.name
        %i
          =basket.user.email
      %br
      %small.text-muted=basket.status_name
