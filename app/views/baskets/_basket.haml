=link_to edit_basket_path(basket), class: 'list-group-item' do
  .media
    .media-body
      %h4.media-heading
        =basket.user.name
      %i
        =basket.user.email
      %br
      %small.text-muted=basket.status_name
